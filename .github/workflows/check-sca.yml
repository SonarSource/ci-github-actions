---
name: Required SCA Check
on:
  pull_request_target:
  merge_group:

permissions:
  id-token: write
  contents: read

jobs:
  verify-sca:
    runs-on: ubuntu-latest
    steps:
      - uses: SonarSource/ci-github-actions/check-sca@master
